server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/DemoDB"
    username: root
    password: 123456789
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true